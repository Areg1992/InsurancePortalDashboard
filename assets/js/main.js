document.addEventListener("DOMContentLoaded", function(event) {
    // if((document.querySelector('.modal-text').length === 'undefined' )) {
    //     document.querySelector('.read-more-btn').remove()
    //
    // }else{
    //     console.log(document.querySelector('.modal-text').length)
    // }
});

